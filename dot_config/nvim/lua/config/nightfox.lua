-- Default options
require('nightfox').setup({
  options = {
    -- Compiled file's destination location
    compile_path = vim.fn.stdpath("cache") .. "/nightfox",
    compile_file_suffix = "_compiled", -- Compiled file suffix
    transparent = false,     -- Disable setting background
    terminal_colors = true,  -- Set terminal colors (vim.g.terminal_color_*) used in `:terminal`
    dim_inactive = false,    -- Non focused panes set to alternative background
    module_default = true,   -- Default enable value for modules
    colorblind = {
      enable = false,        -- Enable colorblind support
      simulate_only = false, -- Only show simulated colorblind colors and not diff shifted
      severity = {
        protan = 0,          -- Severity [0,1] for protan (red)
        deutan = 0,          -- Severity [0,1] for deutan (green)
        tritan = 0,          -- Severity [0,1] for tritan (blue)
      },
    },
    styles = {               -- Style to be applied to different syntax groups
      comments = "italic",     -- Italicize comments to differentiate them from code
      conditionals = "bold",   -- Bold conditionals for better visibility
      constants = "italic",    -- Constants can be italicized for distinction
      functions = "bold",      -- Bold functions for quick identification
      keywords = "bold,italic",-- Keywords in bold and italic for emphasis
      numbers = "NONE",        -- Numbers in standard formatting
      operators = "NONE",      -- Operators in standard formatting
      strings = "italic",        -- Strings in standard formatting, as they are usually distinct
      types = "bold",          -- Bold types for clarity in type definitions and usage
      variables = "NONE",      -- Variables in standard formatting for a cleaner look
    },
    inverse = {             -- Inverse highlight for different types
      match_paren = false,
      visual = false,
      search = false,
    },
    modules = {             -- List of various plugins and additional options
      -- ...
    },
  },
  palettes = {},
  specs = {},
  groups = {},
})

-- setup must be called before loading
vim.cmd("colorscheme nightfox")
